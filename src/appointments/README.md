# Appointments

Tenant-scoped appointment scheduling API backing the existing Calendar & Appointment OS frontend.

The domain persists appointments, trainer availability rules, and trainer time-off. Calendar responses merge appointments with existing PT sessions so the frontend has one scheduling feed without duplicating PT-session records.
